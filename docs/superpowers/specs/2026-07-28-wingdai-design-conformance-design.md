# Spec — ทำ UI ให้ตรง design handoff (Wingdai App.dc.html)

วันที่ 2026-07-28 · แตกจากสาขา `feat/merchant-menu`

---

## 1. เป้าหมาย

ทำให้แอปตรงกับ design handoff `Wingdai App.dc.html` (โปรเจกต์ claude.ai design `fd98a85f-d87c-482c-b7c8-cdab136afa79`)
ทั้งตำแหน่งปุ่ม ระยะ สี และเอฟเฟกต์ พร้อมยกฟีเจอร์ที่คุยกันไว้ใน `claude.md` มาประกอบเข้ากับ UI นั้น

**สิ่งที่ spec นี้ให้:** หลักการตัดสินเมื่อ design ขัดกับ claude.md · บัญชีจอที่จะทำ/ตัด · โมเดล navigation ใหม่ ·
การแบ่งงานเป็น 5 คลื่น · รายละเอียดคลื่นที่ 1

**สิ่งที่ spec นี้ไม่ให้:** รายละเอียดระดับ pixel ของแต่ละจอ — อ่านจาก markup ตอนลงมือทำแต่ละจอ (§8 W1-0)

---

## 2. หลักการตัดสิน

| แหล่ง | เป็นความจริงเรื่อง |
|---|---|
| `Wingdai App.dc.html` | **ภาพ** — layout, ตำแหน่ง, ระยะ, สี, รัศมี, เงา, เอฟเฟกต์, ทรานซิชัน |
| `claude.md` | **ฟีเจอร์และ flow** — มีอะไรใน Phase 1, โมเดลบัญชี, กฎธุรกิจ, กฎเงิน |
| `docs/design/wingdai-design-system.md` | **การแปลงเป็นโค้ด** — token, primitive, กฎ contrast |

**กฎเหล็กที่ไม่เปลี่ยน:** ห้ามใส่ UI ที่กดแล้วไม่มีอะไรเกิดขึ้น — ถ้า design มีองค์ประกอบที่ระบบยังไม่รองรับ ตัดออกก่อน

**กฎ contrast ไม่ผ่อน:** `#F15A22` = `brandAccent` กราฟิกล้วน · `#CC4310` = `brandSolid` พื้นที่มีตัวหนังสือ ·
`#B23A0C` = `brandLink` ตัวอักษรสีแบรนด์ เทสต์ `__tests__/theme/contrast.test.ts` ล็อกไว้ ห้ามลดเกณฑ์ให้ผ่าน

---

## 3. การตัดสินใจจากเซสชัน 2026-07-28

| # | ประเด็น | ตัดสิน |
|---|---|---|
| D1 | จอที่ตรงกับฟีเจอร์ต้องห้าม Phase 1 | **ตัด** Deals/โค้ดส่วนลด, Wallet/แต้ม, Group order, โปรโมชันร้าน · **แก้ 2026-07-28 รอบสอง: C18 เลือกวิธีจ่ายกลับเข้ามา** |
| D2 | design ขัด claude.md เรื่อง auth/role | **ยึด claude.md** — ตัด Superadmin, merchant ยังเป็น capability ไม่ใช่ตัวเลือกตอนสมัคร |
| D3 | identifier ตอน login | **username หรือเบอร์โทร** · **อีเมลยังเก็บตอนสมัคร** แต่ไม่ใช้ login เพราะ username สั้นและง่ายกว่า |
| D4 | OTP | **ตอนสมัคร + ตอนลืมรหัสผ่าน** — ไม่ใช่ทุกครั้งที่ login |
| D13 | Google | **ทำ OAuth ให้ใช้ได้จริง** · คนใหม่: Google สำเร็จ → ฟอร์มสั้น (username + เบอร์) → OTP → เลือกบทบาท |
| D14 | วิธีจ่าย | **PromptPay + เงินสดปลายทาง + บัตร** · PromptPay เป็นตัวที่เลือกไว้ล่วงหน้าเสมอ |
| D5 | ปุ่มกลาง navbar ⭐ | **ไอคอนแฮมเบอร์เกอร์** ขึ้นเฉพาะตอนมีออเดอร์ที่ยังไม่จบ → กดไปจอติดตาม · เวลาปกติ navbar มีแค่ 4 แท็บ |
| D6 | ตะกร้า | **ปุ่มกลมส้มลอยมุมขวาล่างเหนือ navbar + badge จำนวน** ตามภาพ C1 (ไม่ใช่แถบยาว) |
| D7 | จอติดตาม C6 | **แผนที่จริง + รายละเอียดออเดอร์** — MapLibre + Protomaps |
| D8 | Expo Go | **เลิกใช้ ทำ development build** เพราะ MapLibre เป็น native module |
| D9 | จอ A5 | **ใช้หน้าตาเดียวกัน 2 ที่** — ตอนสมัคร (สั่งอาหาร/Deliver & earn) และตอนสลับบทบาทหลัง login |
| D10 | แท็บ | **Home · Menu · History · Me** ตาม design — ไม่มีแท็บกล่องข้อความ |
| D11 | แจ้งเตือน | **กระดิ่งขวาบนหัวจอ Home** + จุดสีเมื่อมีอันที่ยังไม่อ่าน |
| D12 | in-app chat | **ทำทั้ง 3 จุด** (C10 ไรเดอร์, M10 ร้าน, support ใน C24/C25) — ผู้ใช้ยืนยันแม้ `claude.md §2` ระบุห้าม |

### ข้อสันนิษฐานเดียวที่เหลือ — ทักท้วงได้ถ้าไม่ใช่

**A1 · เบอร์โทรยังใช้ login ได้** — คุณระบุ "username/เบอร์" ตอนแรก แล้วรอบสองอธิบายว่าใช้ username
"เพราะสั้นและง่ายกว่า" ซึ่งเป็นการเทียบกับอีเมล ไม่ใช่กับเบอร์ ผมจึงทำ **login = username หรือ เบอร์**
ถ้าอยากให้เหลือ username อย่างเดียวบอกได้

### 2 ข้อที่ขัด claude.md และต้องแก้เอกสาร

ทั้งสองข้อผู้ใช้ได้รับแจ้งข้อขัดแย้งแล้วและยืนยันให้ทำ — บันทึกไว้ให้ชัดว่าเป็นการตัดสินใจ ไม่ใช่ความพลาด

| ข้อ | ขัดกับ | ต้องแก้ |
|---|---|---|
| D12 in-app chat | `§2` มี "in-app chat" ในลิสต์ห้ามสร้าง | ย้ายออกจาก §2 → บันทึกใน §10 พร้อมขอบเขต (แชตผูกกับออเดอร์ ไม่ใช่ DM ทั่วไป) |
| D14 บัตรเครดิต | `§2` มี "Card payments" ในลิสต์ห้ามสร้าง · `§6.5` ระบุ PromptPay เป็นวิธีเดียวของ Phase 1 · `§6.5` เตือนว่าบัตร 3.2–3.65% vs PromptPay 0.8–1.8% กระทบ unit economics | ย้ายออกจาก §2 → แก้ §6.5 ให้รับ 3 วิธี → บันทึกใน §10 |

**สิ่งที่ยังต้องทำตามเดิมแม้เพิ่มบัตรแล้ว** — `§3` ข้อ 5 ยังบังคับ: PromptPay อยู่บนสุดของลิสต์และถูกเลือกไว้
ล่วงหน้าเสมอ ห้ามทำให้บัตรเป็นทางที่ง่ายที่สุด · และ `§6.5` ยังบังคับให้แยกค่าธรรมเนียมตามวิธีจ่ายในรายงาน
กำไรภายใน — เก็บ `paymentMethod` ลงออเดอร์ตั้งแต่คลื่นที่ 1 เพื่อไม่ต้องย้อนมาเติมทีหลัง

---

## 4. บัญชีจอ — 58 → 50

| กลุ่ม | design | ทำ | ตัด |
|---|---|---|---|
| Auth | 6 | 6 | — |
| Customer | 25 | 22 | C16 Deals, C22 Wallet, C23 Group order |
| Merchant | 10 | 9 | M8 โปรโมชันร้าน |
| Rider | 7 | 7 | — |
| Admin | 4 | 4 | — |
| Superadmin | 4 | 0 | ทั้งหมด (D2) |
| System | 2 | 2 | — |
| **รวม** | **58** | **50** | **8 จอ + 1 ส่วน** |

C17 Checkout ทำ แต่ **ตัดบรรทัดส่วนลด/ช่องกรอกโค้ดออก**

**C18 เลือกวิธีจ่าย (D14)** — 3 ตัวเลือก เรียงตามนี้เสมอ ห้ามสลับ:

| ลำดับ | วิธี | สถานะใน Phase 1 |
|---|---|---|
| 1 | **PromptPay QR** — เลือกไว้ล่วงหน้า | mock ตาม `claude.md §10` (ยังไม่เลือก gateway, `§11` ข้อ 3) |
| 2 | เงินสดปลายทาง | ใช้ได้จริง ไม่ต้องมี gateway — ข้าม webhook ไป `accepted` ตรง ๆ |
| 3 | บัตรเครดิต/เดบิต | mock — ต้องมี gateway ที่รองรับบัตรก่อนจึงใช้ได้จริง |

ตัด **"เพิ่มบัตร"** และ **TrueMoney** ออกจาก design · เก็บ `paymentMethod` ลงออเดอร์ตั้งแต่คลื่นที่ 1

### จอที่แอปมีแต่ design ไม่มี — เก็บไว้ วาดด้วยภาษาภาพเดียวกัน

1. **ลืมรหัสผ่าน** — `claude.md §4.2` บังคับให้มี
2. **เลือกตัวเลือกเมนู (add-ons)** — ระบบมี `OptionGroup` แล้ว design ไม่มีจอนี้ฝั่งลูกค้า
3. **สั่งสำเร็จ** — ตัวคั่นระหว่าง checkout กับจอติดตาม

### สถานะปัจจุบัน

**รวมจอที่ต้องมีทั้งหมด = 50 จาก design + 3 จอเสริม = 53 จอ**
ทำแล้ว 16 จอ (ต้องขัดให้ตรง design) · ใหม่ 37 จอ

---

## 5. โมเดล navigation ใหม่

### 5.1 แท็บ (D10)

```
เดิม:  หน้าแรก · ประวัติ · กล่องข้อความ · โปรไฟล์
ใหม่:  Home · Menu · History · Me
```

- **Menu** = C15 หมวดหมู่อาหาร (กริด 2 คอลัมน์ + จำนวนร้านต่อหมวด) → ต้องสร้างในคลื่นที่ 1 เพราะเป็นแท็บ
- **กล่องข้อความ/แจ้งเตือน** ออกจากแท็บ ยังเป็นจอบน stack เข้าจากกระดิ่งหัวจอ Home (D11)
- แท็บที่เลือก: แผ่นส้มมนรองไอคอน + ป้ายชื่อใต้ไอคอน (ตามภาพ C1)

### 5.2 ปุ่มลอย 2 ปุ่ม เงื่อนไขต่างกัน

```
เวลาปกติ                มีออเดอร์ที่ยังไม่จบ         มีของในตะกร้า
                                                            ┌───┐
                                                            │🛒²│ ← มุมขวาล่าง
                              ● ← แฮมเบอร์เกอร์             └───┘
┌───────────────┐     ┌───────●───────┐     ┌───────────────┐
│🏠  🍜  🕐  👤│     │🏠  🍜  🕐  👤│     │🏠  🍜  🕐  👤│
└───────────────┘     └───────────────┘     └───────────────┘
   ไม่มีปุ่มลอย
```

| ปุ่ม | ตำแหน่ง | เงื่อนไขแสดง | กดแล้ว |
|---|---|---|---|
| ออเดอร์ (แฮมเบอร์เกอร์) | กึ่งกลาง คร่อมขอบบน navbar ขอบขาว | มีออเดอร์สถานะ `created`/`accepted`/`preparing`/`picked_up` | → จอติดตาม C6 |
| ตะกร้า | มุมขวาล่าง เหนือ navbar | ตะกร้ามีอย่างน้อย 1 รายการ | → จอตะกร้า C4 |

ทั้งสองปุ่มเป็นอิสระต่อกัน — โผล่พร้อมกันได้

### 5.3 จอติดตาม C6 (D7)

แผนที่ MapLibre เต็มพื้นหลัง + แผ่นข้อมูลลอยด้านล่าง ประกอบด้วย
วงนับถอยหลัง ETA · ไทม์ไลน์สถานะออเดอร์ · การ์ดร้าน · สรุปรายการและยอดแยกค่าอาหาร/ค่าส่ง/ค่าบริการ

**ยังไม่มีไรเดอร์จริงจนถึงคลื่นที่ 4** → คลื่นที่ 1 แสดงหมุดร้านกับหมุดปลายทางและเส้นทางระหว่างสองจุด
ไม่ใส่หมุดไรเดอร์ปลอมที่ขยับเอง เพราะเป็นข้อมูลหลอก การ์ดไรเดอร์จะโผล่เมื่อ `riderId` มีค่าจริง

### 5.4 หัวจอ Home และกระดิ่งแจ้งเตือน (D11)

แถวหัวจอเรียงจากซ้ายไปขวา: kicker `DELIVER TO` ส้ม + ที่อยู่ + ลูกศรลง (ชิดซ้าย) · ปุ่มกระดิ่ง · อวาตาร์

- **ปุ่มกระดิ่ง** — วงกลมขาว เงานุ่ม ไอคอนกระดิ่งเส้น · **จุดส้มมุมขวาบนเมื่อมีแจ้งเตือนที่ยังไม่อ่าน**
  (จุดล้วนไม่มีตัวเลข ไม่มีตัวหนังสือทับ จึงใช้ `brandAccent` ได้)
- **อวาตาร์** — วงกลม `tealSolid` อักษรย่อชื่อสีขาว

### 5.5 จอแจ้งเตือน C20

หัวข้อ `Notifications` กับลิงก์ `Mark all read` สี `brandLink` อยู่บรรทัดเดียวกัน ·
แบ่งกลุ่ม `TODAY` / `EARLIER` เป็น kicker สี `textFaint` ·
การ์ดขาวมุม 20: ชิปไอคอนซ้าย + หัวข้อหนา + เนื้อความ `textMuted` + เวลา `textFaint` ·
การ์ดที่ยังไม่อ่านมีจุดส้มมุมขวาบน

**⚠️ เนื้อหาตัวอย่างใน design ใช้ไม่ได้:** การ์ด "You've earned 1,240 points",
"260 more points until a free delivery" และท้ายข้อความ "you earned 45 points"
เป็นระบบแต้มกับส่งฟรี ซึ่งถูกตัดไปแล้วตาม D1 และ `claude.md §2`/`§3 ข้อ 3`
→ **เอาหน้าตาการ์ด ไม่เอาเนื้อหา** ชนิดแจ้งเตือนที่ระบบรองรับจริงใน Phase 1:

| ชนิด | ตัวอย่าง | มีข้อมูลรองรับตั้งแต่ |
|---|---|---|
| สถานะออเดอร์เปลี่ยน | ร้านรับออเดอร์แล้ว · กำลังทำ · ไรเดอร์รับของแล้ว · ส่งถึงแล้ว | คลื่น 1 |
| ชวนให้คะแนน | ส่งถึงแล้ว ให้คะแนนร้านและไรเดอร์ | คลื่น 2 |
| ร้านใหม่ใกล้คุณ | ร้านใหม่เปิดห่าง 1.2 กม. | คลื่น 2 |
| ผลอนุมัติร้าน/ไรเดอร์ | ร้านของคุณผ่านการอนุมัติแล้ว | คลื่น 3/4 |
| ความคืบหน้าเรื่องคืนเงิน | เรื่องที่แจ้งไว้ได้ข้อสรุปแล้ว | คลื่น 5 |

คลื่นที่ 1 รองรับแค่ชนิดแรก — ชนิดที่เหลือเพิ่มตามคลื่นที่ทำฟีเจอร์นั้นจริง ไม่ใส่การ์ดปลอมไว้ล่วงหน้า

### 5.6 จอ A5 ใช้ 2 ที่ (D9)

| บริบท | ตัวเลือก |
|---|---|
| ตอนสมัคร | สั่งอาหาร / Deliver & earn → `account_type` = `user` หรือ `rider` |
| สลับบทบาท: rider | สั่งอาหาร / Deliver |
| สลับบทบาท: เจ้าของร้านที่อนุมัติแล้ว | สั่งอาหาร / ร้านอาหาร |
| สลับบทบาท: ลูกค้าธรรมดา | ไม่แสดงจอนี้เลย |

หน้าตาเดียวกัน: การ์ดมีชิปไอคอน + หัวข้อ + คำอธิบาย + วงกลมเลือก (ที่เลือกเป็นวงส้มติ๊ก ขอบการ์ดส้ม) + ปุ่ม Continue

---

## 6. การแบ่งเป็น 5 คลื่น

53 จอใหญ่เกินกว่าจะเป็นแพลนเดียว แต่ละคลื่นมี spec + plan + สาขาของตัวเอง

| คลื่น | ได้อะไร | จอ | รายการ |
|---|---|---|---|
| **1** | โครง navigation ใหม่ + ปุ่มลอย 2 ปุ่ม + จอติดตาม + แท็บ Menu + auth ปรับใหม่ + ขัด 16 จอเดิม | 21 | 16 จอเดิม + C2 ค้นหา, C5 PromptPay, C6 ติดตาม, C15 หมวดหมู่, C18 เลือกวิธีจ่าย |
| 2 | Customer ที่เหลือ | 12 | A1 splash, A6 intro, C9 ที่อยู่, C10 แชตไรเดอร์, C11 ให้คะแนน, C12 ตั้งค่า, C13 ตะกร้าว่าง, C14 ใบเสร็จ, C19 รายการโปรด, C21 แก้โปรไฟล์, C24 แจ้งปัญหา, C25 ช่วยเหลือ |
| 3 | Merchant | 7 | M1 แดชบอร์ด, **M2 รับออเดอร์ ⭐ นับถอยหลัง**, M3 คิวออร์เดอร์, M5 รายได้, M6 เปิดร้าน, M9 รีวิว, M10 แชตลูกค้า |
| 4 | Rider + auto-dispatch `§6.3` | 7 | R1 ออนไลน์, **R2 รับงาน ⭐ นับถอยหลัง**, R3 กำลังส่ง, R4 รายได้, R5 สมัครไรเดอร์, R6 ประวัติงาน, R7 โซน |
| 5 | Admin exception-based + คืนเงินกึ่งอัตโนมัติ `§6.4` | 6 | Admin ×4 + System ×2 |

M4 จัดการเมนู กับ M7 เพิ่มเมนู ทำไปแล้ว จึงอยู่ในกลุ่ม "16 จอเดิม" ของคลื่นที่ 1 ไม่ใช่คลื่นที่ 3

**กฎ glass/blur ตลอดทุกคลื่น:** ห้ามใช้ `expo-blur` บนจอคิวออร์เดอร์ร้าน (คลื่น 3), จอรับงานไรเดอร์ (คลื่น 4)
และจอที่มีแผนที่ทุกจอ — ใช้พื้นทึบ ตาม `claude.md §10`

---

## 7. ผลกระทบและความเสี่ยง

| เรื่อง | ผลกระทบ | จัดการ |
|---|---|---|
| เลิกใช้ Expo Go (D8) | ต้อง build ลงเครื่องก่อนทดสอบ ไม่ใช่สแกน QR | ติดตั้ง `expo-dev-client` ครั้งเดียวในคลื่นที่ 1 |
| tile ของ Protomaps | ยังไม่มี backend ให้ host `.pmtiles` | คลื่นที่ 1 **ฝัง `.pmtiles` ของโซนเดียวเป็น asset ในแอป** — โซนหนาแน่น 1–1.5 กม. ไฟล์เล็กมาก ไม่ต้องมี host และเข้ากับกลยุทธ์โซนเดียวของ `§1` พอดี · ย้ายไป host จริงตอนมีหลายโซน |
| แท็บเปลี่ยนชื่อ/จำนวน | เทสต์เดิมที่อ้าง `tab-Inbox` พัง | แก้เทสต์พร้อมกันในคลื่นที่ 1 |
| in-app chat (D12) | ขัด `claude.md §2` และต้องมี backend realtime | แก้ `claude.md` ก่อนเริ่มคลื่นที่ 2 · เฟสแรกใช้ mock repo แบบเดียวกับที่ทำอยู่ |
| ฟีเจอร์ที่ตัด | design มี 8 จอที่จะไม่ถูกสร้าง | บันทึกไว้ใน `docs/design/wingdai-design-system.md §6` ให้ตรงกับ spec นี้ |
| **Google client ID** | ไม่มี = ทำ OAuth ไม่ได้ ต้องได้จาก Google Cloud Console เท่านั้น | **ขอจากเจ้าของโปรเจกต์ก่อนเริ่ม W1-9** · ส่วนอื่นของคลื่นที่ 1 ไม่ต้องรอ |
| บัตรเครดิต (D14) | ขัด `§2`/`§6.5` และต้องมี gateway ที่รองรับบัตร ซึ่ง `§11` ข้อ 3 ยังไม่ตัดสิน | คลื่นที่ 1 เป็น mock เหมือน PromptPay · แก้ `claude.md` ตาม §3 · **ต้องแยกค่าธรรมเนียมตามวิธีจ่ายในโครง ledger ตั้งแต่แรก** ไม่งั้นต้องรื้อ |
| เงินสดปลายทาง | ไรเดอร์ต้องถือเงินทอน และมีความเสี่ยงออเดอร์ผี | คลื่นที่ 4 (rider) ต้องมีขั้นยืนยันรับเงินก่อนปิดออเดอร์ · จดไว้เป็นข้อกำหนดของคลื่นนั้น |
| C13 ตะกร้าว่าง เข้าถึงยาก | ปุ่มตะกร้าโผล่เฉพาะตอนมีของ (D6) → ตะกร้าว่างแทบไม่มีทางเปิดเจอ | ยังทำ C13 ในคลื่นที่ 2 เพราะเข้าได้จากการลบรายการสุดท้ายทิ้งขณะอยู่ในจอตะกร้า |

---

## 8. คลื่นที่ 1 — รายละเอียด

สาขา `feat/design-conformance` แตกจาก `feat/merchant-menu`

### W1-0 · ดึงไฟล์ design ลง repo

ดึง `Wingdai App.dc.html` ผ่าน DesignSync ลง `docs/design/` — **ทำในเซสชันแยกที่ไม่มีงานอื่น**
เพราะไฟล์รวม 58 จอไว้ก้อนเดียว กินบริบทมาก ทำครั้งเดียวจบแล้วทุกเซสชันหลังจากนี้ `grep` เอาได้ฟรี
ดึง `Wingdai Explorations.dc.html` ด้วยถ้าขนาดไหว

**เกณฑ์ผ่าน:** `grep -c 'C1 Home' "docs/design/Wingdai App.dc.html"` ได้ผลลัพธ์ และ `git status` เห็นไฟล์

### W1-1 · เปลี่ยนชุดแท็บเป็น Home · Menu · History · Me

- `CustomerTabParamList`: `CustomerHome` · `Categories` · `Orders` · `Profile`
- `InboxScreen` ย้ายจากแท็บไปเป็นจอบน `CustomerStack` และวาดใหม่เป็น C20 ตาม §5.5 (มีแค่ชนิด "สถานะออเดอร์เปลี่ยน")
- สร้าง `CategoriesScreen` (C15) — กริด 2 คอลัมน์ หมวดจาก `CuisineCategory` + จำนวนร้านต่อหมวด กดแล้วกรองรายชื่อร้าน
- คีย์ i18n ใหม่ทั้งชุด ทั้งไทยและอังกฤษ

**เกณฑ์ผ่าน:** เทสต์ยืนยันว่ามี 4 แท็บชื่อถูกต้อง · แตะแท็บ Menu แล้วเห็นหมวดครบทุกค่าใน `CuisineCategory`

### W1-2 · `useActiveOrder()`

hook อ่านจาก `useCustomerOrders` เดิม คืนออเดอร์ที่สถานะอยู่ในเซต `created`/`accepted`/`preparing`/`picked_up`
เซตนี้ต้องมาจาก `orderStateMachine.ts` ไม่ใช่ลิสต์ที่พิมพ์ซ้ำ เพื่อไม่ให้หลุดเมื่อเพิ่มสถานะใหม่

**เกณฑ์ผ่าน:** เทสต์ครบทุกสถานะ — 4 สถานะแรกคืนออเดอร์ · `delivered`/`cancelled` คืน `undefined`

### W1-3 · `WingdaiTabBar` ใหม่

- เอาปุ่มตะกร้ากลางแถบออก
- เพิ่มปุ่มออเดอร์กลางแถบ ไอคอนแฮมเบอร์เกอร์ ขอบขาว แสดงตามเงื่อนไข §5.2
- เพิ่มปุ่มตะกร้ากลมมุมขวาล่างเหนือ navbar + badge จำนวน แสดงตามเงื่อนไข §5.2
- แท็บที่เลือก: แผ่นส้มมนรองไอคอน + ป้ายชื่อใต้ไอคอน
- ไอคอนใหม่ใน `ui/Icon.tsx`: `burger` · `bowl` · `bell`
- `TAB_BAR_CLEARANCE` ต้องพอสำหรับปุ่มลอย ไม่ให้ทับเนื้อหาท้ายจอ

**เกณฑ์ผ่าน:** เทสต์ 4 กรณี — ไม่มีออเดอร์ไม่มีตะกร้า (เห็นแค่ 4 แท็บ) · มีออเดอร์ · มีตะกร้า · มีทั้งคู่
พื้นที่แตะทุกปุ่ม ≥ 44px

### W1-4 · จอติดตาม C6 + แผนที่

- ติดตั้ง `expo-dev-client` + `@maplibre/maplibre-react-native` + config plugin ใน `app.json`
- ฝัง `.pmtiles` โซนเดียวเป็น asset
- `OrderTrackingScreen` ตาม §5.3 · route บน `CustomerStack`
- วงนับถอยหลัง ETA ด้วย `react-native-svg` ที่มีอยู่แล้ว
- ห้ามใช้ blur บนจอนี้

**เกณฑ์ผ่าน:** เทสต์ render จอด้วยออเดอร์ mock แล้วเห็นสถานะ ยอดแยก 3 ก้อน และชื่อร้าน (mock ตัวแผนที่ใน jest)
เปิดแอปจริงบน dev build แล้วแผนที่ขึ้น

### W1-5 · จอ A5 สองบริบท

`RoleSwitcher.tsx` และ `ChooseAccountTypeScreen.tsx` ใช้ component การ์ดตัวเลือกร่วมกันตาม §5.6

**เกณฑ์ผ่าน:** เทสต์ 4 กรณีตามตาราง §5.6 — ลูกค้าธรรมดาต้องไม่เห็นจอนี้

### W1-6 · ขัด 16 จอเดิมให้ตรง design

ไล่ทีละจอ เทียบกับ markup ที่ได้จาก W1-0 แก้ตำแหน่ง ระยะ ลำดับ และเอฟเฟกต์

**C1 Home — องค์ประกอบตามภาพ เรียงบนลงล่าง:**
หัวจอตาม §5.4 · ช่องค้นหาขาวมุมมนเต็มความกว้าง (ไอคอนแว่นซ้าย) ·
การ์ดประกาศ `tealSolid` มุม 24 มี kicker `ANNOUNCEMENT` สี `brandLink` + หัวข้อขาว + บรรทัดรอง
และวงกลมน้ำตาลล้นขอบขวาเป็นลูกเล่น · แถวชิปหมวด (ชิปที่เลือกพื้น `brandSolid` ตัวขาว
ชิปอื่นพื้นขาว) · หัวข้อ `Nearby` + ลิงก์ `See all` สี `brandLink` ·
การ์ดร้าน 2 คอลัมน์: บล็อกรูปมี badge ระยะทางพื้นเข้มมุมซ้ายบน ใต้รูปมีชื่อร้าน
ครัวซีน · ★ คะแนน แล้วบรรทัดล่างเป็น เวลา · ค่าส่ง กับปุ่ม `+` กลมส้มชิดขวา

รวมกระดิ่งแจ้งเตือน + จุดสีบนหัวจอ Home (D11) และจอ C20 (§5.5)

**เกณฑ์ผ่าน:** ทุกจอไม่มี hex ดิบ · ตัวอักษรผ่าน `ui/Text` ทั้งหมด · ตรวจทั้งโหมดสว่างและมืด ·
ไม่มีปุ่มหรือช่องที่แตะแล้วไม่เกิดอะไร

### W1-7 · C2 ค้นหา และ C5 PromptPay

**C2 ต้องอยู่คลื่นที่ 1** เพราะ C1 มีช่องค้นหาเด่นกลางจอ ถ้าไม่มีปลายทางก็ผิดกฎ "ห้าม UI ที่กดแล้วไม่มีอะไรเกิดขึ้น"
→ จอค้นหาที่กรองร้านและเมนูจาก seed ที่มีอยู่ ไม่ต้องมี backend search

**C5** แยกจอ QR PromptPay พื้น `tealSolid` ออกจาก Checkout ตาม design —
**ไม่ใส่ตัวนับถอยหลังหมดอายุ** เพราะยังไม่มี gateway จริง (`claude.md §11` ข้อ 3 ยังค้าง) จะเป็นตัวนับหลอก

**เกณฑ์ผ่าน:** พิมพ์คำค้นแล้วรายการกรองถูกทั้งชื่อร้านและชื่อเมนู · ค้นไม่เจอมีสถานะว่างที่บอกอะไรบางอย่าง ·
จอ C5 ไม่มีตัวนับถอยหลัง

### W1-8 · C18 เลือกวิธีจ่าย (D14)

ลิสต์เลือกได้ทีละอันตาม §4 · เข้าจาก C17 Checkout แถว "จ่ายด้วย" · เลือกแล้วกลับไป C17 พร้อมค่าใหม่ ·
เพิ่มฟิลด์ `paymentMethod` ใน `Order` และ `CreateOrderInput`

**เกณฑ์ผ่าน:** เทสต์ยืนยัน PromptPay เป็นค่าตั้งต้นและอยู่ลำดับแรก · เลือกได้ทีละอัน ·
ออเดอร์ที่สร้างมี `paymentMethod` ตรงกับที่เลือก · เลือกเงินสดแล้วไม่เข้าจอ QR

### W1-9 · Auth ปรับตาม D3/D4/D13

- **login** รับ username หรือเบอร์ (ตัดอีเมลออกจาก identifier) · อีเมลยังกรอกตอนสมัครเหมือนเดิม
- **ลืมรหัสผ่าน** เปลี่ยนเป็นเส้นทาง OTP ทาง SMS ไปเบอร์ที่ยืนยันแล้ว
- **Google OAuth ใช้ได้จริง** — `expo-auth-session` · คนใหม่ต่อด้วยฟอร์มสั้น (username + เบอร์) → OTP →
  เลือกบทบาท · คนที่ผูกไว้แล้วเข้าตรง
- เบอร์ยังเป็น verified channel เดียวตาม `claude.md §4.2` — Google ไม่ทดแทน OTP

**ไลบรารี:** `@react-native-google-signin/google-signin` — เอกสาร Expo v57 ระบุชัดว่า **ห้ามใช้
`expo-auth-session` สำหรับ Google** ให้ใช้ไลบรารีเฉพาะทางแทน · ต้องมี dev build อยู่แล้วตาม D8 จึงไม่มีต้นทุนเพิ่ม

**สถานะ Google Cloud (2026-07-28):**

| ของ | สถานะ |
|---|---|
| โปรเจกต์ `Wingdai` (`wingdai-503804`, org `euro21-2004-org`) | ✅ สร้างแล้ว |
| OAuth consent screen — External, testing mode | ✅ ตั้งแล้ว |
| Web client ID | ✅ `604454119763-53piv5sil6qe42p69pjcpmntou3s86q5.apps.googleusercontent.com` |
| iOS client ID | ✅ `604454119763-km1m49afqj081oin5tincocas48111o5.apps.googleusercontent.com` |
| Android client ID | ✅ `604454119763-l58er0h5kabndjqpqruvstrturtv5k97.apps.googleusercontent.com` |

**ค่าที่ต้องตั้งให้ตรงกันใน `app.json` (ยังไม่ได้ทำ — ตอนนี้ยังเป็น `mobile` ทั้งไฟล์ ทั้งที่ `claude.md §10`
สั่งให้ใช้ Wingdai ทุกที่):**

| key | ค่า |
|---|---|
| `name` | `Wingdai` |
| `slug` | `wingdai` |
| `ios.bundleIdentifier` | `com.wingdai.app` |
| `android.package` | `com.wingdai.app` |
| `scheme` | `wingdai` |
| plugin `iosUrlScheme` | `com.googleusercontent.apps.604454119763-km1m49afqj081oin5tincocas48111o5` |

`webClientId` ที่ส่งให้ `GoogleSignin.configure()` คือ **Web client ID** ไม่ใช่ของ iOS/Android

**⚠️ SHA-1 ที่ลงทะเบียนไว้เป็นของ debug keystore เครื่องพัฒนาเท่านั้น**
(`5E:E4:B3:A6:61:CE:10:5D:35:AC:EC:77:A3:02:65:34:40:6E:25:FD`, `~/.android/debug.keystore`)
ตอนขึ้น Play Store ต้องกลับไป **เพิ่ม** SHA-1 ของ release keystore ในหน้า Android client เดิม ไม่ใช่สร้างใหม่ —
ถ้าลืม Google sign-in จะทำงานปกติตอนเทสต์แต่พังทันทีบนเครื่องที่โหลดจากสโตร์

client ID เป็นค่าสาธารณะ เก็บใน `app.json` ได้ ไม่ต้องใช้ `.env` ·
**client secret ของ Web client ห้ามเข้า repo และแอปมือถือไม่ต้องใช้**

**เกณฑ์ผ่าน:** เทสต์ login ด้วย username ผ่าน · ด้วยเบอร์ผ่าน · ด้วยอีเมลไม่ผ่าน ·
ลืมรหัสผ่านส่ง OTP แล้วตั้งรหัสใหม่ได้ · คนใหม่ผ่าน Google ต้องผ่านฟอร์มสั้น + OTP ก่อนเข้าแอป

### เกณฑ์ผ่านของทั้งคลื่น

`npx jest` เขียวทั้งหมด · `npx tsc --noEmit` สะอาด (รันจาก `apps/mobile`) · เปิดแอปจริงบน dev build ได้

---

## 9. สิ่งที่ต้องแก้ในเอกสารเดิม

1. **`claude.md §2`** — ย้าย **in-app chat** (D12) และ **card payments** (D14) ออกจากลิสต์ห้ามสร้าง
2. **`claude.md §4.2`** — login identifier เปลี่ยนจาก "username หรืออีเมล" เป็น **"username หรือเบอร์"** ·
   อีเมลยังเก็บตอนสมัครแต่ไม่ใช้ login · เพิ่ม Google เป็นเส้นทางเข้า · ลืมรหัสผ่านใช้ OTP ทาง SMS (D3/D4/D13)
3. **`claude.md §6.5`** — จากเดิม "PromptPay เป็นวิธีเดียวของ Phase 1" เป็น **3 วิธี** โดย PromptPay เป็นค่าตั้งต้น ·
   คงข้อกำหนดเรื่องแยกค่าธรรมเนียมตามวิธีจ่ายไว้ (D14)
4. **`claude.md §10`** — บันทึกเพิ่ม: ตัด Superadmin · แท็บ 4 ตัวตาม design · ปุ่มลอย 2 ปุ่มตามเงื่อนไข ·
   เลิกใช้ Expo Go ไป dev build · in-app chat · card payment · Google OAuth
5. **`claude.md §11`** — ข้อ 3 (payment gateway) **เร่งขึ้น** เพราะตอนนี้ต้องรองรับบัตรด้วย ·
   เพิ่มข้อใหม่: Google OAuth client ID · เพิ่มข้อใหม่: host `.pmtiles` เมื่อขยายเกิน 1 โซน
6. **`docs/design/wingdai-design-system.md §5`** — เขียนแถบนำทางใหม่ตาม §5.2
7. **`docs/design/wingdai-design-system.md §6`** — ปรับลิสต์ "ไม่เอาจาก handoff" ให้ตรง §4 ของ spec นี้
   (แชต บัตร และจอเลือกวิธีจ่าย กลับมาแล้ว)
