# Wingdai — Mobile App

React Native + Expo (SDK 57), TypeScript. แอปเดียวหลาย role ตาม capability (ลูกค้า / ร้าน / ไรเดอร์ / แอดมิน) — ดู `docs/product-spec.md` ที่ราก repo สำหรับบริบทสินค้า/สถาปัตยกรรม

## รันแอป

```bash
cd apps/mobile
npm install
npm test         # jest ทั้งหมด
```

**ต้องใช้ dev build ไม่ใช่ Expo Go** เพราะ MapLibre กับ Google sign-in เป็น native module

```bash
npx expo prebuild --platform ios --clean   # ครั้งแรก หรือหลังแก้ app.config.ts
npx expo run:ios                            # build + เปิด simulator
```

### เลือกว่าจะใช้ข้อมูลจำลองหรือ API จริง

ตัวแปรเดียว — ไม่ตั้ง = ใช้ข้อมูลจำลองในเครื่อง แอปจึงเปิดได้เสมอแม้ไม่มีเซิร์ฟเวอร์

```bash
# ข้อมูลจำลอง (ค่าเริ่มต้น)
npx expo run:ios

# ต่อ core-api จริง — ต้องสั่ง npm run dev ที่ services/core-api ไว้ก่อน
EXPO_PUBLIC_WINGDAI_API_URL=http://localhost:3000/api npx expo run:ios
```

> **เครื่องจริงต้องใช้ IP ในวงแลน** ไม่ใช่ `localhost` เพราะ localhost บนมือถือคือตัวมือถือเอง
> `EXPO_PUBLIC_WINGDAI_API_URL=http://192.168.1.42:3000/api`

## บัญชีทดสอบ

| Username | ประเภทบัญชี | เข้าแอปแล้วเจอ |
|---|---|---|
| `somchai` | user | ลูกค้าทั่วไป → หน้าสั่งอาหาร (CustomerStack) · มีที่อยู่บันทึกไว้แล้ว 2 ที่ |
| `malee` | user + เจ้าของร้าน | เริ่มที่โหมดร้าน (MerchantStack) เพราะเป็นเจ้าของ "ครัวมาลี" ที่อนุมัติแล้ว · สลับเป็นโหมดลูกค้าได้ |
| `rider_ann` | rider (อนุมัติแล้ว) | โหมดไรเดอร์ (RiderStack) · สลับเป็นโหมดลูกค้าได้ |
| `rider_new` | rider (รออนุมัติ) | เห็นแค่หน้า "รออนุมัติ" เข้า stack อื่นไม่ได้ |
| `admin_root` | admin | โหมดผู้ดูแล (AdminStack) — สร้างจาก seed เท่านั้น ไม่มีทางสมัคร |
| `super_root` | super_admin | โหมดตั้งค่าแพลตฟอร์ม (SuperAdminStack) — ราคา โซน สิทธิ์ ประวัติการกระทำ |

**รหัสผ่าน:** ข้อมูลจำลองใช้ `1234` · API จริงใช้ `wingdai1234` (เกณฑ์จริงคือ 8 ตัวขึ้นไป)

ช่อง "เข้าสู่ระบบ" รับ **username หรือเบอร์โทร** — **อีเมลใช้ล็อกอินไม่ได้** (`docs/product-spec.md` §4.2)
อีเมลเก็บไว้เป็นช่องทางรีเซ็ตรหัสอย่างเดียว

**รหัส OTP:** ข้อมูลจำลองใช้ `123456` · API จริงสุ่มหกหลักแล้ว**พิมพ์ลง log ของเซิร์ฟเวอร์**
(ยังไม่ได้เลือกผู้ให้บริการ SMS — `docs/product-spec.md` §11 ข้อ 3) ดูได้ที่หน้าต่างที่รัน `npm run dev`

> Merchant/Rider/Admin/SuperAdmin stack ใช้งานได้จริงทั้งหมดแล้ว ส่วนที่ยังไม่มีคือจอปลีกย่อย
> ฝั่งลูกค้าและแชท — รายการที่ยังไม่ทำอยู่ในหัวข้อ "Not done yet" ของ README หลัก

## ร้าน/เมนูใน seed (ใช้ทดสอบการสั่งอาหาร)

| ร้าน | หมวด | สถานะ | เจ้าของ | หมายเหตุ |
|---|---|---|---|---|
| ครัวมาลี | ข้าว | เปิด | `malee` | มี 4 เมนูพร้อมสั่ง (1 เมนู "หมด") |
| ส้มตำแซ่บนัว | ส้มตำ | เปิด | (ร้านอื่น) | สั่งได้ปกติ |
| ก๋วยเตี๋ยวเรือ | ก๋วยเตี๋ยว | **ปิด** | (ร้านอื่น) | เข้าดูเมนูได้ แต่กดเพิ่มลงตะกร้าไม่ได้ |
| ร้านรออนุมัติ | ข้าว | ปิด | `somchai` | **ไม่อนุมัติ** → ไม่โผล่ในหน้าลูกค้า |
| อีก 19 ร้าน | ครบทั้ง 5 หมวด | ส่วนใหญ่เปิด | (ร้านอื่น) | ร้านละ 3 เมนู · จานสุดท้ายของทุกร้าน "หมด" |

**สามร้านในนั้นอยู่นอกรัศมี 5 กม. โดยตั้งใจ** (ข้าวต้มสีลม · ราเมนทองหล่อ · ส้มตำบางนา) —
`docs/product-spec.md` §7 ให้ร้านเปิดที่ไหนในไทยก็ได้ สิ่งที่จำกัดคือ**ระยะต่อออร์เดอร์** ร้านชุดนี้จึงเป็น
ของจริงที่พิสูจน์ว่าด่านนั้นทำงาน ไม่ใช่ประโยคอธิบายเฉย ๆ — ล็อกอินเป็น `somchai` แล้วค้นหา
"ทองหล่อ" จะไม่เจอ ทั้งที่ร้านนั้นมีอยู่ในฐานข้อมูลจริง

**ทดสอบกฎกันโกง:** ล็อกอินเป็น `malee` (เจ้าของครัวมาลี) แล้วลองสั่งจาก "ครัวมาลี" — ระบบจะบล็อกที่ตอนกดยืนยันชำระเงิน (สั่งร้านตัวเองไม่ได้)
